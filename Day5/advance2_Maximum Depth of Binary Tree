class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(root== NULL)
        return 0;

        int lefthandside= maxDepth(root->left);
        int righthandside= maxDepth(root->right);
        
        return 1+max(lefthandside,righthandside);
    
    }

};
