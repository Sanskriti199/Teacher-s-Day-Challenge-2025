#include <iostream>
#include <string>

using namespace std;

int main() {
    string s;
    cin >> s;
    int length = s.length();
    int result = (length + 1) * 26 - length;
    cout << result << endl;
    return 0;
}
