#include <iostream>

using namespace std;

int main() {
    int a,  c = 0;
    cin >> a;
    for (int i = 0; i < a; ++i) {
        int p, q;
        cin >> p >> q;
        if (q - p >= 2) {
            c++;
        }
    }
    cout << c << endl;
    return 0;
}
